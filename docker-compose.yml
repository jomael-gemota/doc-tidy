version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: doc-tidy-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: doc-tidy

volumes:
  mongo_data:
